<%= simple_form_for @external_organization do |f| %>
  <%= f.input :externalOrganizationName, label: "External Organization Name", required: true %>
  <%= f.button :submit, "Save", class: "btn btn-primary" %>
  <%= f.button :button, "Cancel", type: :reset, class: "btn btn-none" %>
<% end %>